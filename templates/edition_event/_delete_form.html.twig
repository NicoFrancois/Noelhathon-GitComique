<form method="post" action="{{ path('edition_event_delete', {'id': edition_event.id}) }}" onsubmit="return confirm('Confirmer la suppression?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ edition_event.id) }}">
    <button class="btn">Effacer</button>
</form>
