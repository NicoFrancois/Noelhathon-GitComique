{% extends 'base.html.twig' %}

{% block title %}EditionEvent index{% endblock %}

{% block body %}
    <h1>EditionEvent index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for edition_event in edition_events %}
            <tr>
                <td>{{ edition_event.id }}</td>
                <td>{{ edition_event.date ? edition_event.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('edition_event_show', {'id': edition_event.id}) }}">show</a>
                    <a href="{{ path('edition_event_edit', {'id': edition_event.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('edition_event_new') }}">Create new</a>
{% endblock %}
