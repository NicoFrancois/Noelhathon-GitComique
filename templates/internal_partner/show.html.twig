{% extends 'base.html.twig' %}

{% block title %}InternalPartner{% endblock %}

{% block body %}
    <h1>InternalPartner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internal_partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ internal_partner.name }}</td>
            </tr>
            <tr>
                <th>EventCount</th>
                <td>{{ internal_partner.EventCount }}</td>
            </tr>
            <tr>
                <th>PartnerIntake</th>
                <td>{{ internal_partner.partnerIntake }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('internal_partner_index') }}">back to list</a>

    <a href="{{ path('internal_partner_edit', {'id': internal_partner.id}) }}">edit</a>

    {{ include('internal_partner/_delete_form.html.twig') }}
{% endblock %}
