{% extends 'base.html.twig' %}

{% block title %}EditionService{% endblock %}

{% block body %}
    <h1>EditionService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edition_service.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edition_service_index') }}">back to list</a>

    <a href="{{ path('edition_service_edit', {'id': edition_service.id}) }}">edit</a>

{% endblock %}
