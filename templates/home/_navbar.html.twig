<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path('home') }}"><img class='logoLabo' src="{{ asset('build/images/lab-o-logo.png') }}" alt="logo Labo"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav fullSite text-left mx-auto mr-auto">

                <a class="nav-link text-align: center" href="{{ path('service_index') }}">Services</a>
                <a class="nav-link text-align: center" href="{{ path('event_index') }}">Évenements</a>
                <a class="nav-link text-align: center" href="{{ path('start_up_index') }}">Start'Up</a>

        </ul>
    </div>
</nav>