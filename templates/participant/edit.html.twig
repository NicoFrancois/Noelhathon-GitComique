{% extends 'base.html.twig' %}

{% block title %}Modifier Participant{% endblock %}

{% block body %}
    <h1>Modifer Participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('participant_index') }}">Retour à la liste</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
